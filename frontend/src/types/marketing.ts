export type MarketingSummary = {
  reach: string;
  reachChange: string;
  impressions: string;
  impressionsChange: string;
  ctr: string;
  ctrChange: string;
  totalCost: string;
  remainingBudget: string;
};

export type MarketingChannelDetail = {
  platform: string;
  reach: string;
  ctr: string;
  cost: string;
  cpc: string;
};